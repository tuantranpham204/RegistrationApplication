import { IJetRouter } from "../interfaces";
import { HashRouter } from "./HashRouter";
export declare class UrlRouter extends HashRouter implements IJetRouter {
    protected _detectPrefix(): void;
    protected _getRaw(): string;
}
