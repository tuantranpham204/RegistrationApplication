import { IJetURL, IJetView, IPath, IRoute, IJetURLChunk, IJetUrlTarget } from "./interfaces";
export declare class Route implements IRoute {
    route: IPath;
    private index;
    private _next;
    constructor(route: string | IPath, index: number);
    current(): IJetURLChunk;
    next(): IJetURLChunk;
    suburl(): IJetURL;
    shift(params: any): Route;
    setParams(url: any, params: any, index: any): void;
    refresh(): void;
    toString(): string;
    _join(path: string, kids?: boolean): IJetURL;
    append(path: string): string;
    show(path: IJetUrlTarget, view: IJetView, kids?: boolean): Promise<void>;
    size(n: number): void;
    split(): IRoute;
    update(name: string, value: string, index?: number): any;
}
