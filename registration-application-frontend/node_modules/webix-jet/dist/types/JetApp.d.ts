import { IJetApp } from "./interfaces";
import { JetAppBase } from "./JetAppBase";
export declare class JetApp extends JetAppBase implements IJetApp {
    constructor(config: any);
    require(type: string, url: string): any;
}
